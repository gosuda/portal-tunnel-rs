[package]
name = "xtask"
version = "0.0.0"
description = "Workspace task runner — codegen, release, openapi-export, dep-audit, refresh-frontend-bundle."
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish = false

[lints]
workspace = true
