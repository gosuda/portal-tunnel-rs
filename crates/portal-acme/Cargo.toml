[package]
name = "portal-acme"
version = "0.1.0"
description = "ACME issuance with DNS-01 across local, Cloudflare, Route53, and Google Cloud DNS."
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[lints]
workspace = true
